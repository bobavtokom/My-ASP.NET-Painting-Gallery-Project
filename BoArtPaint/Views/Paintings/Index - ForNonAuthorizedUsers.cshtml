@model IEnumerable<BoArtPaint.Models.Painting>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table id="paintings" class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaintUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Artist.ArtistName)
            </th>
            <th>
                <p>Buttons</p>
            </th>
        </tr>
    </thead>
    <tbody>*@
<div class="=row">
    @foreach (var item in Model) {
        <div class="col-md-4 col-sm-6 col-lg-3 gallery" id="paintings">
            <p class="modal-title">@item.Name</p>
            <a href="paintings/details/@item.Id">
                <img src="@item.PaintUrl" class="img-thumbnail">
            </a>
            @Html.ActionLink("More", "Details", "Paintings", new { id = item.Id }, new { @class = "btn btn-primary" })
        </div>
    }

</div>

@*<tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PaintUrl)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Price)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Artist.ArtistName)
                    </td>
                    <td>
                        <button data-painting-id="@item.Id" class="btn-danger js-delete">Delete</button>

                    </td>
                </tr>
            }
        </tbody>


    </table>*@

